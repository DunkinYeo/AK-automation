# Runtime
flask==3.1.0
Appium-Python-Client==4.0.0
selenium==4.22.0
PyYAML==6.0.2
APScheduler==3.10.4
requests==2.32.3
jinja2==3.1.4
# IANA timezone DB for Windows — without this, APScheduler crashes at startup
# on Windows hosts ("No time zone found with key <local tz>")
tzdata==2026.2

# iOS automation (Mac hosts only; harmless elsewhere)
pymobiledevice3==9.33.0   # WDA launch, device listing, port forward, app install
Pillow==12.3.0            # pixel-based UI state detection (Connect button, stepper)

# Dev / formatting
black==24.10.0
ruff==0.8.6
pre-commit==4.0.1
